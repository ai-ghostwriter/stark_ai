# Claude Code Skills

This directory contains the two FRIDAY/JARVIS persona skills used by Claude Code.

- `FRIDAY.md` for the concise, critical operational persona.
- `JARVIS.md` for the formal, analytical persona.

Both skills map to the same role chain:

`Claude Architect` -> `Codex Implementer` -> `Claude Reviewer`
